# Filled in from real `hyprctl monitors` output on the actual 3x 1080p
# desktop (gamdias) — matched the placeholder's example values exactly.
monitor=DP-1,1920x1080@60,0x0,1
monitor=DP-2,1920x1080@60,1920x0,1
monitor=HDMI-A-1,1920x1080@60,3840x0,1

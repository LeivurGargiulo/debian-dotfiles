Ensures ~/.config/gh-dash/ exists before wallbash tries to write
config.yml into it — fn_wallbash skips a template whose destination
directory doesn't exist yet (see dotfiles/.config/hyde/wallbash/theme/gh-dash.dcol).

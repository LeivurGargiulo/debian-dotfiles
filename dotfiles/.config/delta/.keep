Ensures ~/.config/delta/ exists before wallbash tries to write
theme.gitconfig into it — fn_wallbash skips a template whose destination
directory doesn't exist yet (see dotfiles/.config/hyde/wallbash/theme/delta.dcol).

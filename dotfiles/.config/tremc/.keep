Ensures ~/.config/tremc/ exists before wallbash tries to write
settings.cfg into it — fn_wallbash skips a template whose destination
directory doesn't exist yet (see dotfiles/.config/hyde/wallbash/theme/tremc.dcol).

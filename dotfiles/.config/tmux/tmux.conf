# Colors live in ~/.config/tmux/theme.conf, regenerated by wallbash from
# whichever HyDE theme is active (see dotfiles/.config/hyde/wallbash/theme/tmux.dcol).
# -q silences the "no such file" warning before the first theme apply.
source-file -q "$HOME/.config/tmux/theme.conf"

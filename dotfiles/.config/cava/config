# Colors are managed by HyDE's own wallbash integration
# (~/.config/hyde/wallbash/always/cava.dcol + scripts/cava.sh), which
# appends a generated [color] block below the marker line it inserts here
# on every `hydectl theme set` / wallpaper switch -- do not add a [color]
# section above this file, it would be a second, conflicting one.

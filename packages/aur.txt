# --- AMD GPU monitor ---
amdgpu_top

# --- CLI/TUI tools without an official-repo package ---
ncspot
bandwhich
bluetuith
glow
ducker
taskwarrior-tui
gophertube
# wden dropped: no such AUR package ("No AUR package found for wden" in a
# real install run). yay only warns and moves on, so it never failed the
# install, but it was a permanent no-op line. bitwarden-cli-bin + rbw below
# already cover the Bitwarden side.
ytfzf
tremc

# --- password / 2FA ---
rbw
rofi-rbw
bitwarden-cli-bin

# --- dev tools (unofficial AUR builds) ---
claude-code

# --- Python libraries (AUR-only) ---
python-questionary

# --- shell environment ---
oh-my-zsh-git
tmux-plugin-manager

# --- browsers / chat (AUR -bin builds) ---
rustdesk-bin

# --- gaming / disk tools (AUR -bin builds) ---
steam-tui-bin
ventoy-bin
osu-lazer-bin
prism-tui-bin

# --- games ---
nsnake
bastet
2048-cli-git
brogue-ce

# --- dashboards ---
gh-dash-bin
wtfutil-bin

# --- music addons ---
sptlrx-bin

# --- dev tools, no official-repo package ---
posting
ptpython
frogmouth

# --- Monokai Pro cursor/icon theme build tooling ---
python-clickgen
papirus-folders

# --- fonts ---
ttf-ubuntu-sans

# --- ratatui TUI picks, no official-repo package (github.com/ratatui/awesome-ratatui) ---
# mirro-rs-git dropped: fails to link against system rustc (rust-lld vs
# ring crate hidden-symbol mismatch, unresolved even overriding rustflags
# via env/cargo config). Managing mirrorlist via reflector/CLI instead.
parui
journalview
hwatch
doxx-git
bookokrat-bin

# --- TUI replacements for GUI apps ---
claude-squad-bin
endcord
nchat
vm-curator-bin

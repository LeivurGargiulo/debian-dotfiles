---
- name: Apply chezmoi source state
  ansible.builtin.command: chezmoi init --source "{{ playbook_dir }}/../chezmoi" --apply
  become: true
  become_user: "{{ invoking_user }}"
  environment:
    HOME: "{{ invoking_home }}"
